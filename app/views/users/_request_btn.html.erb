<%= simple_form_for @friendship do |f| %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.hidden_field :friend_id, :value => event.id %>
  <%= f.button :submit, "Attend", class: "btn-success float-right mt-2" %>
<% end %>
